"use client";
/**
 * @deprecated Import from `@/shared/components/TimeRangeSegments` instead.
 * This file is kept as a thin re-export so existing Overview-page imports
 * continue to resolve while consumers migrate to the canonical location.
 */
export {
  TimeRangeSegments,
  type TimeRangeValue,
  type CustomRange,
} from "@/shared/components/TimeRangeSegments";